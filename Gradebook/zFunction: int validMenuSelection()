int validMenuSelection()
{
    int uInput;
    while (scanf_s("%d", &uInput) != 1 || uInput < 1 || uInput > 6)
    {
        printf("\n");
        printf("Invalid input. Select 1 - 6\n");
		while (getchar() != '\n'); 

    }
    return uInput;
}
